#include "main.h"
/**
 * main - A function that passes Betty checks.
 *
 * Return: Returns 0 on success otherwise on Error.
 */
int main(void)
{
	char *str;

	str = "Hello World";
	printf("%s", str);

	return (0);
}
